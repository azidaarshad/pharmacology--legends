NSNT LEGENDS — FINAL ONE SET

Cloudflare files:
- worker.js (main Worker; serves the matching index.html embedded inside it)
- index.html (matching frontend copy)
- wrangler.jsonc (D1 binding DB; preserve the existing database)
- assets/ (22 topic visuals + OnMyWay.mp3)

Upload/replace worker.js and index.html together. Keep the existing D1 database/binding DB; do not create a new D1 migration just for this package.

Player login: full matric number, e.g. DPNS1/2026(05)-0001, then choose Team A-F.
MCQ 45s; SEQ/Clinical 120s.
